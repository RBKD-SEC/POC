[project]
name = "poc"
version = "0.1.0"
description = "PoC capability repository (pocsuite3 + standalone)"
requires-python = ">=3.9"
dependencies = [
    "jsonschema>=4.21",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
